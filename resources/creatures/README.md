# Criaturas

Cada bichinho é uma pasta com um `manifest.json` + spritesheets PNG (grid 96×96 por frame — ver
`docs/adr/frontend/0003-sprites-via-spritesheet-configuravel.md`).

Assets reais ainda não existem — `placeholder/manifest.json` documenta o formato esperado.
Enquanto isso, o renderer usa um placeholder de emoji (ver `src/renderer/src/App.tsx`).
